{% for activities  in activity %}

	<div class="col-md-4">
		<div class="card mb-4 shadow-sm">
			<div class="card-body">
				<h3 class="card-text">{{ activities.name }}</h3>
			</div>
		</div>
	</div>

{% endfor %}
